import React from "react";
import SampleContainer from "../containers/1831-SampleContainer";
const SampleApp = () => {
    return (
        <div>
            <SampleContainer />
        </div>
    );
};

export default SampleApp;
